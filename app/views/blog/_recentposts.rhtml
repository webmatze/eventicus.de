<% for post in get_recent_blogposts %>
<ul class="recent-posts">
	<li>
		<%= link_to strip_tags(post.title), :controller => 'blog', :action => 'show', :id => post.id %> 
		<span class="date"><%= post.created_at.strftime('%m/%d/%y') %></span>
	</li>
</ul>
<% end %>
